version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.4
    steps:
      - checkout
      - run:
          command: |
            sudo pip install pipenv
            pipenv install
      - run:
          - command: |
              - sudo pip install praw
      - run:
          command: |
            pipenv run python PIBotTests.py
